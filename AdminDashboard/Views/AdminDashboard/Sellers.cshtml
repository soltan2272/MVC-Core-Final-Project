@using Models;
@using PagedList;
@model IPagedList<User>;
@{
    ViewData["Title"] = "Sellers";

}


<div class="all-content-wrapper">

    <div class="product-status mg-b-30">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                    <div class="product-status-wrap">
                        <h4>Products List</h4>
                        @*<div class="add-product">
            @Html.ActionLink("Add Product", "AddProduct")

            <a href="product-edit.html"></a>
        </div>*@
                        <table>
                            <tr>
                                <th>Full_Name</th>
                                <th>Email</th>
                                <th>Adress</th>
                                <th>Phone</th>
                                <th>SSN</th>
                                <th>Date of birth</th>
                                <th>Delete</th>

                            </tr>

                            @foreach (User usr in Model)
                            {
                                <tr>
                                    <td>@usr.Full_Name</td>
                                    <td>@usr.Email </td>
                                    <td>@usr.Adrress </td>
                                    <td>@usr.Phone </td>
                                    <td>@usr.SSN </td>
                                    <td>@usr.Date_Of_Birth </td>
                                    <td>
                                        @Html.ActionLink("Delete", "Delete", new { ID = usr.Id }, new { @class = "btn btn-danger" })

                                    </td>
                                </tr>
                            }



                        </table>
                        <div class="custom-pagination">

                            <ul class="pagination">

                                @for (int i = 1; i <= Model.PageCount; i++)
                                {
                                    <li class="@(i == Model.PageNumber ? "page-item active" : "page-item")">
                                        <a class="page-link" href="@Url.Action("Sellers", new { p = i })">@i</a>
                                    </li>

                                }

                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>

<style>
    .checked {
        color: orange;
    }
</style>